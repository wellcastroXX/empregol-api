node_modules/
dist/

# Variáveis de ambiente (NUNCA commitar segredos)
.env
.env.*
!.env.production.example

# Dados do PostgreSQL (bind mount no servidor)
pgdata/

# Firebase Admin service account (SEGREDO — nunca commitar)
*-firebase-adminsdk-*.json
firebase-service-account.json
secrets/

# Logs / OS
*.log
.DS_Store
